<h1 class="mb-4"><%= @flat.name %></h1>

<div class="container">
  <div class="row">

    <div class="col-md-4 mb-4">
      <div class="card">
        <h5 class="card-title"><%= @flat.name %></h5>
        <p class="card-text">Address: <%= @flat.adress %></p>
        <p class="card-text">Description: <%= @flat.description %></p>
        <p class="card-text">Price per night: € <%= @flat.price_per_night %></p>
        <p class="card-text">Number of guests: <%= @flat.number_of_guests %></p>
        <div>
          <%= link_to "Edit", edit_flat_path, class: "btn btn-success mb-4" %>
          <%= link_to 'Delete Flat', flat_path(@flat), data: { turbo_method: :delete, turbo_confirm: "Are you sure?" }, class: "ms-3 text-danger"%>
      </div>
    </div>

  </div>
  <%= link_to "Back to all flats", flats_path, class: "btn btn-success mb-4" %>
</div>
